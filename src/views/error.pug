extends ./layouts/main.pug
include ./mixin/feedbackModal.pug

block content
  +feedbackModal(
    "error",
    title || "Error",
    error || "Ha ocurrido un error inesperado.",
    "Volver al inicio",
    "/"
  )